name: Test Workflow

on:
  workflow_dispatch:  # Allows you to run it manually from GitHub UI

permissions:
  contents: write  # Needed if you want to commit or push files (optional for now)

jobs:
  test_job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Print Hello
        run: echo "Hello, world!"
      
      - name: Print email
        run: echo "23f3002663@ds.study.iitm.ac.in"
